"""Optional framework integrations for HyperHerd.

Each submodule is self-contained and gated on its host framework being
importable. The base `hyperherd` package has no runtime dependency on
any of them; install the relevant extra (e.g. `pip install
hyperherd[lightning]`) to use one.
"""
